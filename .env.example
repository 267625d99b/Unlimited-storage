# Telegram Bot Token - احصل عليه من @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Telegram Channel ID - معرف القناة الخاصة (يبدأ بـ -100)
TELEGRAM_CHANNEL_ID=-100xxxxxxxxxx

# Server Port
PORT=3000

# Security Settings (Optional)
# Allowed origins for CORS (comma separated, leave empty for all)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Node Environment (development/production)
NODE_ENV=development

# ============ AUTHENTICATION ============
# Secret key for JWT - MUST be changed in production!
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('base64url'))"
# Minimum 32 characters required
JWT_SECRET=your_super_secret_key_change_this_in_production_min_32_chars

# Refresh token secret (optional, defaults to JWT_SECRET + '_refresh')
JWT_REFRESH_SECRET=your_refresh_secret_key_change_this_too

# Admin password - MUST be changed!
# Password requirements:
# - Minimum 8 characters
# - At least one uppercase letter (A-Z)
# - At least one lowercase letter (a-z)
# - At least one number (0-9)
# - At least one special character (!@#$%^&*()_+-=[]{}|;:,.<>?)
ADMIN_PASSWORD=Admin@123!

# Session expiry in hours (default: 24)
SESSION_EXPIRY_HOURS=24

# Enable/disable authentication (true/false)
AUTH_ENABLED=true

# ============ HTTPS (Optional) ============
# To enable HTTPS, place your certificates in server/certs/
# - server/certs/server.key (private key)
# - server/certs/server.crt (certificate)
# 
# Generate self-signed certificate for development:
# openssl req -x509 -newkey rsa:4096 -keyout server/certs/server.key -out server/certs/server.crt -days 365 -nodes -subj "/CN=localhost"

# ============ EMAIL (Optional) ============
# Enable email functionality (verification, password reset)
# Set to true and configure SMTP settings below to enable email features
EMAIL_ENABLED=true

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Email sender
EMAIL_FROM=noreply@cloudstorage.local

# App info for emails
APP_NAME=التخزين السحابي
APP_URL=http://localhost:5173

# ============ 2FA (Two-Factor Authentication) ============
# 2FA is always available but optional for users
# Users can enable it from their account settings

# ============ MONITORING (Optional) ============
# Prometheus metrics endpoint: /metrics
# Grafana default credentials: admin / admin123
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123

# ============ FILE SCANNING (Optional) ============
# Enable file virus/malware scanning
FILE_SCAN_ENABLED=true
# Maximum file size to scan (in bytes, default 10MB)
FILE_SCAN_MAX_SIZE=10485760

# ============ AI PROVIDERS (اختر واحد - كلهم مجانيين) ============
# 
# 🌟 Google Gemini (مجاني 100% - الأفضل)
# احصل على المفتاح من: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# ⚡ Groq (مجاني وسريع جداً)
# احصل على المفتاح من: https://console.groq.com/keys
# GROQ_API_KEY=your_groq_api_key

# 💬 Cohere (مجاني للاستخدام الشخصي)
# احصل على المفتاح من: https://dashboard.cohere.com/api-keys
# COHERE_API_KEY=your_cohere_api_key

# 💰 OpenAI (مدفوع)
# احصل على المفتاح من: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key
